# V2 Results

This directory stores raw V2 outputs and summary tables for the thesis.

- `baselines/` contains classical linear and nonlinear MPC runs
- `learned/` contains learned EDMD-MPC runs
- `summary/` contains aggregate CSV and manifest files used for plotting

Use `word length` terminology everywhere in this tree.

