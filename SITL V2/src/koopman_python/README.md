# koopman_python

Fresh Python reimplementation target for the V2 learned EDMD-MPC stack.

Rules:

- Use MATLAB in this repo as the primary source of truth.
- Use `KoopmanMPC_Quadrotor` only to resolve ambiguity or fill missing logic.
- Do not import or copy the old `SITL` EDMD, quantization, or QP modules.

